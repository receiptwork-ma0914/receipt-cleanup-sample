import {activities,shapeRounds,sortRounds,countRounds,numberWords} from './content.mjs';
import {initialState,reduce,visibleShapes} from './model.mjs';
let state=initialState();
let currentAudio=null;
let audioSequence=0;
const app=document.getElementById('app');
const paths={speaker:'M3 9h4l5-4v14l-5-4H3z M16 8q5 4 0 8 M19 5q8 7 0 14',mute:'M3 9h4l5-4v14l-5-4H3z M17 9l6 6m0-6-6 6',home:'m3 11 9-8 9 8M5 10v11h5v-7h4v7h5V10',replay:'M5 7v6h6M5 12a8 8 0 1 1 2 7',check:'m4 12 5 5L20 5',arrow:'M4 12h15m-6-6 6 6-6 6',help:'M9 7a4 4 0 1 1 6 4c-2 1-3 2-3 4m0 4v1',exit:'M10 3H4v18h6M9 12h13m-5-5 5 5-5 5',plus:'M12 4v16M4 12h16',minus:'M4 12h16',hand:'M8 13V5a2 2 0 0 1 4 0v6-2a2 2 0 0 1 4 0v2a2 2 0 0 1 4 0v6c0 4-3 5-6 5s-5-1-7-4l-3-4a2 2 0 0 1 3-2z'};
const icon=(name)=>`<svg class="icon" viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="${paths[name]||paths.check}"/></svg>`;
const shape=(name,vary=false)=>`<svg viewBox="0 0 100 100" aria-hidden="true"><g fill="#e8b951" stroke="#254f3d" stroke-width="5" transform="${vary?'translate(50 50) rotate(45) scale(.68) translate(-50 -50)':''}">${name==='circle'?'<circle cx="50" cy="50" r="36"/>':name==='square'?'<rect x="16" y="16" width="68" height="68" rx="1"/>':'<path d="M50 12 90 83H10Z"/>'}</g></svg>`;
const flower=(color='red')=>`<svg viewBox="0 0 100 112" aria-hidden="true"><path d="M50 64v44M50 88Q18 92 21 69q23-1 29 19M50 95q5-24 29-23 3 23-29 23" fill="#54734c" stroke="#254f3d" stroke-width="3"/><path d="M50 11C64-6 80 10 74 26 95 23 103 47 84 56 97 72 79 91 64 78 57 99 34 96 34 76 17 87 1 67 17 53-4 42 10 20 27 27 24 6 45-5 50 11Z" fill="url(#${color==='red'?'dots':'stripes'})" stroke="#213e32" stroke-width="3"/><circle cx="50" cy="48" r="12" fill="#f9df8b" stroke="#213e32" stroke-width="3"/></svg>`;
const basket=(color)=>`<svg viewBox="0 0 100 85" aria-hidden="true"><path d="M25 32C25-4 75-4 75 32" fill="none" stroke="#254f3d" stroke-width="6"/><path d="M7 30h86L81 80H19Z" fill="url(#${color==='red'?'dots':'stripes'})" stroke="#254f3d" stroke-width="5"/></svg>`;
const sprout=`<svg viewBox="0 0 80 80" aria-hidden="true"><path d="M40 69V34M40 52Q5 50 11 16q30-4 29 36M40 39q1-29 30-27 5 29-30 27" fill="#79a174" stroke="#254f3d" stroke-width="4" stroke-linecap="round"/><path d="M16 69h48" stroke="#254f3d" stroke-width="5" stroke-linecap="round"/></svg>`;
const definitions=`<svg width="0" height="0" aria-hidden="true" style="position:absolute"><defs><pattern id="dots" width="16" height="16" patternUnits="userSpaceOnUse"><rect width="16" height="16" fill="#a83740"/><circle cx="8" cy="8" r="3" fill="#fff7df"/></pattern><pattern id="stripes" width="14" height="14" patternUnits="userSpaceOnUse" patternTransform="rotate(35)"><rect width="14" height="14" fill="#25688d"/><path d="M3 0v14" stroke="#fff7df" stroke-width="3"/></pattern></defs></svg>`;
const button=(action,text,ic,classes='',extra='')=>`<button type="button" class="btn-icon ${classes}" data-action="${action}" ${extra}>${ic?icon(ic):''}${text}</button>`;
const scene=`<svg class="hero-art" viewBox="0 0 480 380" role="img" aria-label="A sunny garden with circle, square and triangle flower beds"><path d="M32 162C26 64 110 21 213 43c63-71 200-29 212 67 62 104 17 222-121 229C140 367 6 299 32 162Z" fill="#e2eacb"/><circle cx="373" cy="86" r="35" fill="#e8b951"/><path d="M66 113q10-33 36-16 13-38 41-5 31-5 30 24H66" fill="#fffdf6"/><path d="M80 289q132-69 312-7" fill="none" stroke="#68845b" stroke-width="18" stroke-linecap="round"/><path d="M238 327q5-32 35-61t-15-43" fill="none" stroke="#d8bf8e" stroke-width="40"/><g stroke="#254f3d" stroke-width="5"><path d="M115 275v-85m0 47q-36-6-33-32 33-1 33 32m0-14q7-36 34-30-2 32-34 30" fill="#77a16b"/><circle cx="115" cy="156" r="42" fill="#e8b951"/><path d="M241 235v-85m0 43q-30-1-31-25 28-7 31 25" fill="#77a16b"/><rect x="207" y="107" width="70" height="70" rx="3" fill="#db9b8d"/><path d="M354 272v-75m0 46q30 1 35-27-29-2-35 27" fill="#77a16b"/><path d="m355 118 43 78h-86Z" fill="#8bb2bb"/></g><path d="m299 76 7-7 8 7m-21 3 6-6 7 6" fill="none" stroke="#254f3d" stroke-width="3" stroke-linecap="round"/><ellipse cx="128" cy="320" rx="27" ry="9" fill="#c0ccaa"/><path d="M112 316q12-27 31 0" fill="#b3c394"/></svg>`;
function header(){return `<header class="masthead"><div class="brand">${sprout}<span>little garden</span></div><div class="top-actions">${button('mute',`<span class="mobile-label">${state.muted?'Sound off':'Sound on'}</span>`,state.muted?'mute':'speaker','soft',`aria-label="${state.muted?'Turn sound on':'Mute sound'}" aria-pressed="${state.muted}"`)}${button('exit','<span class="mobile-label">Grown-up exit</span>','exit','soft','aria-label="Grown-up exit to start"')}</div></header>`;}
function footer(){return `<footer class="footer"><span>A little practice. Together.<br>No scores. Stop whenever you like.</span>${button('guide','Grown-up guide','','soft')}</footer>`;}
function welcome(){return `<main><section class="hero"><div class="hero-copy"><div class="eyebrow">A small world of early maths</div><h1>Let curiosity<br>take root.</h1><p>Match a shape. Find a basket.<br>Grow a little group of flowers.</p>${button('home','Grown-up: let’s play','arrow','primary')}<p class="start-note">For around age three, with a grown-up nearby.<br>Three little activities. Try one, or explore them all.</p></div>${scene}</section><div class="notice"><strong>Before you begin</strong> · Sit together. Point and tap to show how it works. Sound starts only when you choose an activity; every prompt also has a visual example. Take a break whenever you like.</div></main>`;}
function activityArt(key){return key==='shapes'?shape('triangle'):key==='sorting'?basket('blue'):flower('red');}
function map(){const all=state.done.length===3;return `<main><section class="intro"><div class="eyebrow">${all?'Your garden, explored':'Choose a place to play'}</div><h1>${all?'Look what you explored.':'What shall we grow today?'}</h1><p>Tap a picture. Watch together. Then have a go.</p></section><nav class="cards" aria-label="Garden activities">${Object.entries(activities).map(([key,a])=>`<button type="button" class="activity-card" data-action="start" data-activity="${key}" aria-label="${a.title}${state.done.includes(key)?', explored, play again':''}">${activityArt(key)}<strong>${a.title}</strong><small>${a.short} together · a few minutes</small>${state.done.includes(key)?`<span class="complete-mark">${icon('check')}</span>`:''}</button>`).join('')}</nav>${all?`<section class="summary"><ul>${Object.values(activities).map(a=>`<li>${a.summary}</li>`).join('')}</ul><p><strong>Take the garden with you:</strong> match large paper shapes, sort them into two groups, then count up to three. An adult can point and count with you.</p>${button('reset','Grow a new garden','replay','primary')}</section>`:`<p class="audio-note">The check marks remember places you have explored. They are not a score.</p>`}</main>`;}
function tools(){return `<div class="tools" aria-label="Activity tools">${button('replay','Hear again','speaker','','aria-label="Replay the current spoken prompt"')}${button('hint','Show me','help','','aria-label="Show a visual hint"')}${button('restart','Start again','replay','','aria-label="Reset this activity"')}${button('home','Garden','home','','aria-label="Leave this activity for the garden"')}</div>`;}
function demo(){const key=state.activity; const items=key==='shapes'?[shape('circle'),shape('circle')]:key==='sorting'?[flower('red'),basket('red')]:[flower('blue'),flower('blue')];const text=key==='shapes'?'This circle matches this circle.':key==='sorting'?'A red flower with dots goes in the red basket with dots.':'One flower here. One flower here. They match.';return `<div class="play-area"><p class="prompt">Watch with your grown-up.</p><p class="subtitle">${text}</p><div class="demo-row" role="img" aria-label="${text} Tap the first picture, then its match."><div class="demo-item">${items[0]}<span class="demo-hand">${icon('hand')}</span></div><span class="arrow" aria-hidden="true">→</span><div class="demo-item hinted">${items[1]}<span class="demo-hand">${icon('hand')}</span></div></div><p class="demo-label">Point together. Then tap to try.</p><div class="actions">${button('try','My turn','hand','primary')}</div></div>`;}
function shapes(){const r=shapeRounds[state.round];return `<div class="play-area"><p class="prompt">Find the same shape.</p><p class="subtitle">${r.vary?'A little turn. Still the same shape.':'Look at the shape in the garden bed.'}</p><div class="target"><div class="target-card"><div class="target-label">Match this</div>${shape(r.target)}</div></div><div class="choices" aria-label="Shape choices">${visibleShapes(state).map(s=>`<button type="button" class="shape-choice ${state.hint&&s===r.target?'hinted':''}" data-action="match" data-shape="${s}" aria-label="Choose the ${s}${r.vary?', turned and smaller':''}" ${state.solved?'disabled':''}>${shape(s,r.vary)}</button>`).join('')}</div></div>`;}
function sorting(){const flowers=sortRounds[state.round];return `<div class="play-area"><p class="prompt">Find a matching basket.</p><p class="subtitle">${state.selected===null?'Tap a flower. Then tap a basket.':'Flower chosen. Tap its basket.'}</p><div class="flowers" aria-label="Flowers to sort">${flowers.map((color,i)=>state.placed.includes(i)?`<span class="flower-space" aria-label="Flower placed">${icon('check')}</span>`:`<button type="button" class="flower-choice ${state.selected===i?'selected':''}" data-action="select" data-index="${i}" aria-label="Choose ${color} flower with ${color==='red'?'dots':'stripes'}" aria-pressed="${state.selected===i}">${flower(color)}</button>`).join('')}</div><div class="baskets" aria-label="Sort by colour; patterns also identify each group">${['red','blue'].map(color=>`<button type="button" class="basket-button ${state.hint&&state.selected!==null&&flowers[state.selected]===color?'hinted':''}" data-action="place" data-bin="${color}" aria-label="Place in the ${color} basket with ${color==='red'?'dots':'stripes'}" ${state.solved?'disabled':''}>${basket(color)}<span class="basket-name">${color==='red'?'Red · dots':'Blue · stripes'}</span><span class="basket-items">${state.placed.filter(i=>flowers[i]===color).map(()=>flower(color)).join('')}</span></button>`).join('')}</div></div>`;}
function collection(n,total,current=-1,scatter=false){return `<div class="collection ${scatter?'scatter':''}" role="img" aria-label="${n} visible flower${n===1?'':'s'}${current>=0?`; flower ${current+1} highlighted`:''}">${Array.from({length:total},(_,i)=>`<span class="plant ${i<n?'counted':'empty'} ${i===current?'current':''}" ${i<n?`data-counted-index="${i}"`:''}>${i<n?flower('blue'):''}</span>`).join('')}</div>`;}
function counting(){const target=countRounds[state.round];const ready=state.revealed===target;return `<div class="play-area"><p class="prompt">Grow the same little group.</p><p class="subtitle">${state.round===2?'They are in a new place. Count each flower once.':'Tap to count each flower. Then make a matching garden.'}</p><div class="count-patches"><section class="patch"><h2>Count with me</h2>${collection(state.revealed,target,state.revealed-1,state.round===2)}<div class="count-word" aria-hidden="true">${state.revealed?numberWords[state.revealed]:''}</div>${button('reveal',ready?'All counted':'Count a flower','hand','primary',`aria-label="${ready?'All flowers counted':'Reveal and count the next flower'}" ${ready||state.solved?'disabled':''}`)}</section><section class="patch"><h2>Your garden</h2>${collection(state.made,3)}<div class="count-word" aria-hidden="true">${state.made?numberWords[state.made]:''}</div><div class="count-tools">${button('remove','','minus','',`aria-label="Remove one flower" ${state.made===0||state.solved?'disabled':''}`)}${button('add','Grow','plus','primary',`aria-label="Grow one flower" ${!ready||state.made===3||state.solved?'disabled':''}`)}</div></section></div><div class="actions" style="margin-top:20px">${button('check','Do they match?','check','',`${!ready||state.solved?'disabled':''}`)}</div>${state.hint?`<div class="demo-row" aria-label="Match one flower to one flower"><div class="demo-item">${flower('blue')}</div><span class="arrow" aria-hidden="true">↔</span><div class="demo-item">${flower('blue')}</div></div>`:''}</div>`;}
function complete(){const a=activities[state.activity];return `<section class="summary"><div class="summary-art">${sprout}</div><div class="eyebrow">A little exploring, together</div><h1>Your garden is growing.</h1><p>${a.summary}</p><div class="notice"><strong>Next, away from the screen</strong><br>${a.next} A grown-up can show you.</div><div class="actions" style="margin-top:24px">${button('home','Choose a garden','home','primary')}${button('restart','Play this again','replay')}</div></section>`;}
function activity(){const a=activities[state.activity];const total=state.activity==='shapes'?shapeRounds.length:state.activity==='sorting'?sortRounds.length:countRounds.length;return `<main><div class="lesson-head"><h1>${a.title}</h1><div class="progress" role="img" aria-label="${state.stage==='complete'?'Activity explored':state.stage==='demo'?'Demonstration':`Practice step ${state.round+1} of ${total}`}">${Array.from({length:total},(_,i)=>`<span class="${i<state.round||state.stage==='complete'?'done':i===state.round&&state.stage==='practice'?'current':''}"></span>`).join('')}</div></div><section class="panel">${state.stage==='complete'?complete():state.stage==='demo'?demo():state.activity==='shapes'?shapes():state.activity==='sorting'?sorting():counting()}${state.feedback?`<div class="feedback" role="status">${state.solved?icon('check'):''}<span>${state.feedback}</span></div>`:''}${state.solved&&state.stage==='practice'?`<div class="actions">${button('next',state.round+1===total?'Garden grown':'Try a new one','arrow','primary')}</div>`:''}${tools()}</section><p class="audio-note">Touch or use Tab and Enter. Sound is optional. A grown-up can point and count with you.</p></main>`;}
function guide(){return `<main class="guide"><div class="eyebrow">For grown-ups</div><h1>A little practice, together.</h1><p>Little Garden offers matching, colour sorting and counting practice for approximately age three. Children differ; follow their interest and stop at any time. This is not an assessment or a promise of learning gains.</p><h2>Make space for play</h2><p>Sit alongside your child for one to three minutes per activity. Show the first turn, then let them point or tap. Name what they notice. No reading, typing or spoken answer is required. Sound is optional; every activity has a picture demonstration.</p><h2>Fewer choices</h2><p>Use two shape choices for a simpler start. The matching shape is always included. Sorting always uses two groups; an adult can point out one flower at a time. In counting, start with the one-flower round and stop there if that is enough.</p><div class="settings" aria-label="Number of shape choices">${button('choices','Two choices','','',`data-value="2" aria-pressed="${state.choices===2}"`)}${button('choices','Three choices','','',`data-value="3" aria-pressed="${state.choices===3}"`)}</div><h2>What to explore</h2><ul><li><strong>Shape beds:</strong> identical circle, square and triangle matches, then a smaller turned square. A turn changes its position, not its shape.</li><li><strong>Colour baskets:</strong> two colour groups. Red always has dots; blue always has stripes. These redundant cues support the same two groups without requiring colour vision. Tap a flower, then a basket. The second tray changes the order and group sizes.</li><li><strong>Growing together:</strong> tap once for each flower in a group of one, two or three. Each appears and is highlighted once. Grow a matching collection. The final group is arranged differently; spacing does not change how many.</li></ul><h2>Help without pressure</h2><p>Try “What do you notice?” Point along a shape’s edge. Use the Show me button, or match one real block to each flower. Accept pointing, an adult tapping on the child’s behalf, or simply watching. Avoid timing, scores and correcting every spoken label.</p><h2>Privacy and reset</h2><p>No names, uploads, accounts, tracking or child information. Progress and the two-choice setting are held in memory only. Refreshing clears them. Start again resets the current activity; Reset garden clears all exploration check marks.</p><div class="actions">${button('reset','Reset garden','replay')}${button('home','Back to the garden','home','primary')}</div><h2>References for adults</h2><p><a href="https://familymath.stanford.edu/for-educators/counting-all-about/" target="_blank" rel="noopener noreferrer">DREME Family Math: All About Counting</a><br>Supports one word per object, the last word naming the total, and the same total despite a different counting order. Our reveal button and matching collection make those ideas visible.</p><p><a href="https://www.naeyc.org/node/658" target="_blank" rel="noopener noreferrer">NAEYC: Discovering Shapes and Space in Preschool</a><br>Supports exploring familiar shapes across sizes and orientations with adult conversation. The final shape round turns and shrinks the choices.</p><h2>Away from the screen</h2><p>Use large paper shapes and large blocks with adult supervision. Match a circle, square and triangle. Sort two colours, adding dots and stripes if helpful. Count up to three blocks, spread them out and count again. Avoid small loose objects.</p><p>See the included EDUCATOR_GUIDE.md for full prerequisites, adaptations and limitations. This original lesson has been reviewed through agent-operated simulated use only, not a study with children or human users.</p></main>`;}
function currentPrompt(){if(state.view!=='activity')return 'Choose a picture to explore the garden.';if(state.stage==='complete')return 'You explored the garden. You can play again, or stop and look for shapes around you.';if(state.stage==='demo')return state.activity==='shapes'?'Watch. This circle matches this circle. Now you try.':state.activity==='sorting'?'Watch. Tap a red flower with dots. Then tap the red basket with dots. Now you try.':'Watch. One flower here. One flower in my garden. They match. Now you try.';return state.feedback||activities[state.activity].prompt;}
function stopAudio(){audioSequence++;if(currentAudio){currentAudio.pause();currentAudio=null;}try{window.speechSynthesis?.cancel();}catch{}}
function speak(text,key){stopAudio();const sequence=audioSequence;if(state.muted)return;let usedFallback=false;const fallback=()=>{if(usedFallback||state.muted||sequence!==audioSequence)return;usedFallback=true;try{if('speechSynthesis'in window&&typeof SpeechSynthesisUtterance==='function'){const voice=window.speechSynthesis.getVoices().find(v=>v.localService&&/^en\b/i.test(v.lang));if(!voice)return;const u=new SpeechSynthesisUtterance(text);u.voice=voice;u.rate=.84;u.lang='en-US';window.speechSynthesis.speak(u);}}catch{}};if(key){try{const audio=new Audio(`assets/audio/${key}.wav`);currentAudio=audio;audio.addEventListener('error',fallback,{once:true});audio.play().catch(fallback);}catch{fallback();}}else fallback();}
function promptKey(){if(state.view!=='activity')return null;if(state.stage==='complete')return 'complete';if(state.stage==='demo')return `${state.activity}-demo`;return `${state.activity}-prompt`;}
function render(focus=false,oldKey){app.innerHTML=definitions+`<div class="shell">${header()}${state.view==='welcome'?welcome():state.view==='map'?map():state.view==='guide'?guide():activity()}${footer()}</div>`;if(focus){const h=app.querySelector('main h1');if(h){h.tabIndex=-1;h.focus({preventScroll:true});}}else if(oldKey){const el=[...app.querySelectorAll('button')].find(b=>keyFor(b)===oldKey&&!b.disabled);el?.focus({preventScroll:true});}}
function keyFor(b){return [b.dataset.action,b.dataset.shape,b.dataset.index,b.dataset.bin,b.dataset.value].join('|');}
app.addEventListener('click',e=>{const b=e.target.closest('button[data-action]');if(!b||b.disabled)return;const action=b.dataset.action;if(action==='replay'){speak(currentPrompt(),state.feedback?null:promptKey());return;}const before=state;state=reduce(state,{type:action,activity:b.dataset.activity,shape:b.dataset.shape,index:b.dataset.index===undefined?undefined:Number(b.dataset.index),bin:b.dataset.bin,value:Number(b.dataset.value)});const pageChanged=before.view!==state.view||before.activity!==state.activity||before.stage!==state.stage||before.round!==state.round;render(pageChanged,keyFor(b));if(['mute','exit','home','guide','reset','choices'].includes(action)){stopAudio();return;}let key=pageChanged?promptKey():null;if(action==='reveal'&&state.revealed!==before.revealed)key=numberWords[state.revealed];if(state.solved&&!before.solved)key='success';if((action==='match'||action==='place'||action==='check')&&state.hint)key='retry';speak(currentPrompt(),key);if(state.solved&&!before.solved)app.querySelector('[data-action="next"]')?.focus({preventScroll:true});else if(action==='reveal'&&state.revealed===countRounds[state.round])app.querySelector('[data-action="add"]')?.focus({preventScroll:true});else if(action==='add'&&state.made===3)app.querySelector('[data-action="check"]')?.focus({preventScroll:true});else if(action==='remove'&&state.made===0)app.querySelector('[data-action="add"]')?.focus({preventScroll:true});});
render();
